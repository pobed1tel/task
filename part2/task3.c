#include <math.h>
#include <stdio.h>

int main(){

    // 2 задание
    int x = 2;
    int y = 2;
    int third = x*y

    printf("%d * %d = %d\n", x, y, third);
    printf("%d + %d = %d\n", x, y, x + y);

    return 0;
}