#include <math.h>
#include <stdio.h>

int main(){

    // 10 задание
    int x = 12;
    int y = 4;
    float distance = sqrtf(x*x + y*y);
    printf("Расстояние: %.2f\n", distance);
    
    return 0;
}