#include <math.h>
#include <stdio.h>

int main(){

    // 7 задание
    int a = 100;
    int b = 30;
    int c = 15;
    int sum = a + b + c;
    printf("%d + %d + %d = %d\n", a, b, c, sum);
    float mean = sum / 3;
    printf("Среднее арифметическое: %f\n", mean);
    
    return 0;
}